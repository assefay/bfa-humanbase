# Vues SQL — Déplacements

`01_deplacements.sql` : fonction `norm_txt`, table `gcorr_commune_alias`, fonction `gcorr_resolve_commune`, vue `v_deplacements`.
Appliqué au projet Supabase bfa-humanbase le 24/09/2026 (migration `deplacements_v_deplacements`).
